---
import CarouselGallery from "./../components/CarouselGallery.astro";

const galleryImages = [
  "./Bilder Insta Beitrag/IG-Beitrag-01.png",
  "./Bilder Insta Beitrag/IG-Beitrag-02.png",
  "./Bilder Insta Beitrag/IG-Beitrag-03.png",
];
---

<section id="social" class="SozialMedia" style={`--carousel-height: 420px;`}>
  <div class="event-info">
    <div class="event-info-text">
      <h2>News, Stories & mehr</h2>
      <h3>Let´s stay connected!</h3>

      <CarouselGallery>
        {galleryImages.map((src) => (
          <img src={src} alt="Social Media Beitrag" class="galleryImage" />
        ))}
      </CarouselGallery>
    </div>
  </div>
</section>

<style>
  .SozialMedia {
    padding: var(--spacing-xl) var(--spacing-md);
    flex-wrap: wrap;
    gap: var(--spacing-lg);
  }
  .galleryImage {
    width: 230px;
    max-width: 100%;
    display: block;
    margin: 0 auto;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.18), 0 1.5px 6px rgba(0, 0, 0, 0.12);
    border-radius: 8px;
  }
</style>